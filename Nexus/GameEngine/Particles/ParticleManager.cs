using Nexus.Engine;

namespace Nexus.GameEngine {

	public class ParticleManager {

		public ParticleManager(LevelScene scene) {

			//this.particles = new int[5];

			// .particles[particleId]
			//	.details
			//		.texture, .atlas, .rotationStart, .fadeRules, .whatever...
			//	.values
			//		[0] = x, [1] = y, [2] = rotation?
		}

		public void ReturnToPool() {
			//this.pos.x = -5000;
			//this.pos.y = -5000;
			//delete this.scene.activeObjects[this.loadOrder][this.id];
		}
	}
}
